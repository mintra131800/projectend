import React from 'react'
import {
  Image
} from 'react-native'































/*import propTypes from 'prop-types'
import { View, StyleSheet,Text,Button,Image,TouchableOpacity,SafeAreaView } from "react-native";
 
// style
    const viewStyle={
        backgroundColor: 'red',
        justifyContent :'center',
        alignItems:'center',
        height:60,
        paddingTop:15,
        shadowColor:'#000000',
       marginTop:10,
        shadowOpacity:0.2,
        elavation:2,
        position:'relative'
    };
 
    const textStyle ={
        fontSize : 20
    };
 
// init component
export default  class Header extends Component{

    constructor(props){
        super(props);
        this.headerText=props.headerText
        this.secondText=props.secondText
        this.path=props.path
    }
    render(props){
        return(
        <View>
        <View style={viewStyle}> 
            <Text style={textStyle}>{this.headerText }MIntra</Text>
        </View>
        <View style={viewStyle}> 
        <Text style={textStyle}>{this.secondText}</Text>
    </View>
  <View style={viewStyle}>
   <Image style={{width:50,height:40}} source={this.path} /> 
    </View>
    </View>
        );
    };}*/
